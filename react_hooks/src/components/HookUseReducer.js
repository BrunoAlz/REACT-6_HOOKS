import { useReducer } from "react";

const HookUseReducer = () => {
  return (
    <div>
      {/* 3 - useReducer */}
      <h2>useReducer</h2>

      <hr />
    </div>
  );
};

export default HookUseReducer;
